<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Holds TWO kinds of contract - see README.md before adding anything here.
         This project must never reference a search engine implementation: Bfs.Iop.Core depends on
         it, and so does the engine that implements it. -->
    <ProjectReference Include="..\..\Core\Bfs.Iop.Core.Abstractions\Bfs.Iop.Core.Abstractions.csproj" />
  </ItemGroup>

</Project>
