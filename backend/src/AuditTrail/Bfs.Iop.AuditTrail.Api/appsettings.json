{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "TripleStore": {
        "Endpoint": "#{TRIPLE_STORE_ENDPOINT}",
        "Username": "#{TRIPLE_STORE_USERNAME}",
        "Password": "#{TRIPLE_STORE_PASSWORD}"
    },
    "postgresCredentialsSectionKey": "#{POSTGRES_CREDENTIALS_SECTION_KEY}#"
}
